import 'package:flutter/material.dart';

class ClipImageDemo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return new Scaffold(

      appBar: AppBar(
        title: new Text("圆角图片"),
      ),

      body: new Container(
        alignment: Alignment.center,
        margin: EdgeInsets.all(10),

      ),
    );
  }

}